name: Deploy Enquiry Service

on:
  push:
    tags:
      - 'v*-enquiry'

jobs:
  docker:
    runs-on: ubuntu-latest

    # Run only if this push is BOTH on production branch AND a tag matching v*-api
    if: github.ref_name == 'production' || startsWith(github.ref, 'refs/tags/v') && contains(github.ref, '-enquiry')

    steps:
      # 1. Checkout repo
      - uses: actions/checkout@v3

      # 2. Set Docker image version from tag
      - name: Set Docker image version
        run: |
          VERSION=${GITHUB_REF#refs/tags/}  # e.g., v1.2.3-enquiry
          VERSION=${VERSION#v}              # strip leading 'v' → 1.2.3
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Log version
        run: echo "Docker image version is $VERSION"

      # 3. Docker login
      - uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}

      # 4. Create env file for enquiry-service
      - name: Create env file
        run: |
          mkdir -p env
          echo "PORT=${{ secrets.ENQ_PORT }}" >> env/enquiry-service.env
          echo "DATABASE_URL=${{ secrets.ENQ_DB_URL }}" >> env/enquiry-service.env

      # 5. Build Docker image
      - run: docker build -t mykks32/realstate-enquiry-service:$VERSION ./enquiry-service

      # 6. Push Docker image
      - run: docker push mykks32/realstate-enquiry-service:$VERSION

      # 7. Tag as latest and push
      - run: |
          docker tag mykks32/realstate-enquiry-service:$VERSION mykks32/realstate-enquiry-service:latest
          docker push mykks32/realstate-enquiry-service:latest

      # 8. Calls Render's webhook to deploy the new image
      - name: Trigger Render Deploy
        if: success()
        run: |
          curl -X POST "${{ secrets.RENDER_ENQUIRY_DEPLOY_HOOK }}"
